---
"@channel.io/app-sdk-wam": minor
"@channel.io/app-sdk-wam-ui": patch
---

Add an optional `targetAuthScope` to WAM Function calls so authenticated managers can explicitly select channel-owned or manager-owned OAuth credentials without changing caller identity.
