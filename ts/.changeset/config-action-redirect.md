---
"@channel.io/app-sdk-core": minor
---

Add Config action redirect results and per-action HTTPS redirect origins, with
Proto-backed TypeScript schemas and Go types. Requires App Store support before
applications enable redirects; existing action results remain compatible.
