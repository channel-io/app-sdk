---
"@channel.io/app-sdk-core": patch
---

Expose manager-scoped webhook metadata and AppStore-issued callback URLs in Function context, including the OAuth connected lifecycle fast path and OAuth lifecycle Hook schemas.
